{{>header}}
<div class="container mt-5">
    <div class="text-center">
        <h2>Mis tomas</h2>
    </div>

    <ul class="list-group">
        {{#takes}}
            <li class="list-group-item">
                <a href="/takes/{{id}}" class="text-decoration-none text-primary">{{date}}</a>
            </li>
        {{/takes}}
    </ul>

    {{#logged}}
        <div class="text-center mt-4">
            <button class="btn btn-success" onclick="location.href='/takes/newtake'">Nueva toma</button>
        </div>
    {{/logged}}
</div>

{{>footer}}

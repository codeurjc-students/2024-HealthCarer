<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error General</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel='stylesheet' type='text/css' media='screen' href='../../static/css/error.css'>
    <style>

        body {
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .error-container {
            text-align: center;
            padding: 50px;
            border-radius: 10px;
            background-color: #ffffff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .error-container h1 {
            font-size: 4rem;
            color: #dc3545;
        }

        .error-container p {
            font-size: 1.2rem;
            color: #6c757d;
        }

        .error-container a {
            text-decoration: none;
            color: #ffffff;
            background-color: #007bff;
            padding: 10px 20px;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .error-container a:hover {
            background-color: #0056b3;
        }
        .error-container h1.m{
            font-size: 4rem;
            color: #7cd97c;
        }


    </style>
</head>
<body>
<div class="error-container">
    <h1>¡Ups!</h1>
    <p>Lo sentimos, Tus credenciales no son correctas</p>
    <p>Por favor, inténtalo de nuevo</p>
    <a href="/login" class="btn">Volver a inicio</a>
    <a href="/register" class="btn">Haz clic aquí si no tienes cuenta</a>

</div>

<!-- Bootstrap JS (opcional, solo si necesitas funcionalidad JS) -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
